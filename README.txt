README

-Quick fix the showpass() function in user edit/create to import an addon to your local files
-Create a user in the backend in order to log in and use the app
-Be sure localport numbers are changed if necessary

-Run the SQLData in SQL if you want the specific data from Justin's server